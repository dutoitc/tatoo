package ch.mno.tatoo.facade.commanline;

import java.io.IOException;

import org.junit.Test;

/**
 * Created by dutoitc on 10/08/15.
 */
public class TestTalendCommandLineWrapper {


	@Test
	public void testListJob() throws IOException {
	/*	TelnetWrapper wrapper = Mockito.mock(TelnetWrapper.class);
		Mockito.when(wrapper.getLastData()).thenReturn(IOUtils.toString(getClass().getResourceAsStream("/listJob.txt")));
		List<Job> jobs = new TalendCommandLineWrapper(wrapper).listJobs(Pattern.compile("TEST_.*"));
		for (Job job: jobs) {
			Assert.assertNotNull(job.getFullName());
		}*/
	}

}
